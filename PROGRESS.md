# Progress Log

A session-by-session narrative of what's been built, what's in flight, and what's next. Companion to `git log` (which is the underlying audit trail). Updated at the end of every working session.

## Status

**Step 9 of 10 done.** Step 9 (automated ingest scheduling + on-demand "Sync Sheets" button) landed 2026-06-01: the dashboard header now carries a `RepullButton` that invokes all 6 ingest Edge Functions in parallel via `supabase.functions.invoke()` and shows per-ingest progress + rowsUpserted in a popover, while [migration 019](supabase/migrations/20260601000019_schedule_ingest_cron.sql) wires pg_cron + pg_net to fire each ingest every 15 minutes (staggered by 1 min across the cycle). The button is gated by sign-in only — no role check — since sign-up is admin-provisioned, so any signed-in user is already trusted enough. Only step 10 (polish + Netlify deploy) remains.

Previously: **Step 6 of 10** — Realtor Managers Weekly ingest landed end-to-end (closes step 3). Realtor Management (`/realtor-management`) is now live data: Recruitment Metrics table (6 metric rows × month cols, Jan–Apr 2026 populated) + Activity Measurement table with synthesized **"Weekly Realtor Meeting"** row (merges Master Class 1 + 2; row-drill exposes the 1 vs 2 split) + Stakeholders Meeting row + Monthly Trend chart (stacked bars: New Referrals sky + New Business Reps slate = New Realtors total; per-bar drill into all recruitment + activity metrics for that month) + 3 unchanged out-of-v1 `GreyedCard`s.

## Build order overview

The 10-step roadmap from [PROJECT_BRIEF.md](PROJECT_BRIEF.md):

1. ~~Supabase setup, migrations, RLS, seed reference data~~ ✓
2. ~~Google Sheets API service account setup, share each sheet~~ ✓
3. ~~Ingestion Edge Functions — Bank Deposit ✓, Marketing Expense ✓, Customer Support ✓, Weekly Sales ✓, Customer File ✓, Realtor Managers Weekly ✓~~
4. ~~React scaffold — auth, routing, layout shell, global date filter~~ ✓
5. ~~Sales (Land) section — pattern-setting~~ ✓
6. ~~Marketing → Customer Support → Realtor Management sections~~ ✓ (Realtor live data 2026-05-25)
7. Media & Content (manual-entry forms for the 4 brands)
8. Rule-based narrative engine, per-section, cached per period
9. ~~Automated ingest scheduling + on-demand "Sync Sheets" button~~ ✓ (2026-06-01)
10. Polish, Netlify deploy ← here

## Completed milestones

- **2026-05-07** — Project bootstrap (commit `f1942d3`):
  - Foundation docs: `PROJECT_BRIEF.md`, `DESIGN_DECISIONS.md`
  - Scaffold: Vite + React 18 + TypeScript + Tailwind v3 + Recharts + Supabase JS, all installed via pnpm
  - Supabase CLI initialized (`supabase/config.toml`); migrations + functions dirs created
  - Migration 001 (`20260507000001_extensions_and_helpers.sql`): `uuid-ossp`, `pgcrypto`, `pg_trgm` extensions; `set_updated_at()` trigger function; `parse_month_year()` helper for Marketing Expense source-tab anchoring
  - First commit on `main`, attributed to user identity

- **2026-05-10** — GitHub remote attached and pushed; workflow infrastructure locked in (commit `70570f9`):
  - Repo created at `pgoperations/pertinence-dashboard`; remote configured; push succeeded after collaborator-access fix
  - Migration 002 (`20260510000002_reference_tables.sql`) — reference tables + seed: `brands`, `locations` + aliases, `purposes` + aliases, `expense_categories`, `plot_types` (4 canonical seeded), `realtor_managers` (3 seeded), `customer_service_reps` (5 seeded with brand FKs), `complaint_categories` + aliases. RLS enabled, no policies (deferred to migration 006).
  - Workflow infrastructure: `MEMORY.md` baseline (6 files), this `PROGRESS.md`, critique-workflow integration plan all in place
  - `pgoperations` org → `shawnumo` collaborator path resolved the push 403

- **2026-05-10** — Schema phase end-to-end (migrations 003–007 written this session):
  - Migration 003 (`20260510000003_auth.sql`) — auth: `user_role` enum (admin/editor/viewer), `profiles` table (1:1 with `auth.users`), `handle_new_user` signup trigger, `prevent_role_self_change` BEFORE UPDATE trigger (catches what RLS WITH CHECK can't enforce), helpers `current_user_role()` / `is_admin()` / `is_editor_or_admin()` (SECURITY DEFINER + STABLE + pinned `search_path`), and profile RLS policies
  - Migration 004 (`20260510000004_fact_tables.sql`) — six fact tables: `marketing_expenses`, `bank_deposits`, `weekly_sales`, `customer_files`, `customer_support_logs`, `realtor_manager_weekly`. Common idempotency contract `(source_sheet, source_tab, source_row_id)` unique + `raw_row` jsonb + `quality_flags` jsonb. Foreign keys to reference tables `on delete set null` so canonical changes don't destroy facts.
  - Migration 005 (`20260510000005_aggregates.sql`) — minimum-viable aggregate set: `marketing_monthly`, `customer_support_monthly` (period × brand), `sales_by_location_monthly` (with the partial-index trick to keep the null-location bucket unique per period). More aggregates land per-panel during step 5+.
  - Migration 006 (`20260510000006_rls_policies.sql`) — RLS for reference / fact / aggregate: all `to authenticated`, anon role gets nothing. Reference tables admin-managed; fact + aggregate writes service-role-only.
  - Migration 007 (`20260510000007_dashboard_infra.sql`) — `data_quality_alerts` (editors + admins resolve), `narrative_cache` (service-role writes only, with `generator_version` for cache invalidation), `social_media_manual_entries` (editors + admins write, admins delete).
  - `supabase/functions/_shared/quality_flags.ts` — exported flag vocabulary (`unknown_location`, `unknown_purpose`, `fallback_category`, `missing_realtor`, `low_match_confidence`, `unparseable_plot_type`, `unparseable_date`, `null_sales_person`)

- **2026-05-10** — Phase 1 complete. Migrations 001–008 applied to live Supabase project (`hrmrqpkcvyjwxrehrgvq`) via the dashboard SQL editor. First-admin promotion exposed a bootstrap bug: the `prevent_role_self_change` trigger in migration 003 blocked the very first role assignment because `is_admin()` returns false when no admin exists yet (and `auth.uid()` is null in the SQL editor's postgres-role context). Fixed in migration 008 by short-circuiting the trigger when `auth.uid() is null` — that's only ever true for trusted server-side contexts since anon clients are filtered by the existing profiles RLS policies before the trigger runs. Smoke test passed: test user signed up via dashboard → `handle_new_user` trigger created profile row with `role='viewer'` → promotion to admin succeeded after 008. Public signup disabled in dashboard (Auth → Sign In / Up).

- **2026-05-10** — Phase 2 partial: Google Cloud + service account + Sheets auth path verified.
  - GCP project `pertinence-dashboard` created on the Pertinence Group Google account (clean project ID, no numeric suffix — name was free).
  - Google Sheets API enabled on the project.
  - Service account `dashboard-sheets-reader@pertinence-dashboard.iam.gserviceaccount.com` created with no project-level IAM role (sheet-by-sheet sharing handles auth — more secure).
  - JSON key downloaded, stored outside the repo. `SHEETS_SERVICE_ACCOUNT_EMAIL` + `SHEETS_SERVICE_ACCOUNT_PRIVATE_KEY` set in `.env.local`.
  - All 4 source sheets shared with the service account as Viewer + IDs captured in `.env.local`:
    - Marketing Fund Expense Sheet — `SHEET_ID_MARKETING_EXPENSE`
    - Bank Deposit Mirror — `SHEET_ID_BANK_DEPOSIT` (shared same-day after supervisor approved edit access)
    - MASTER SHEET- CUSTOMER SUPPORT — `SHEET_ID_CUSTOMER_SUPPORT`
    - Marketing Team Reporting Template — `SHEET_ID_REALTOR_MANAGERS_WEEKLY`
  - `scripts/smoke-test-sheets.mjs` created — Node `googleapis`-based reader, run via `pnpm smoke:sheets`. Verified against both Marketing Fund Expense and Bank Deposit Mirror `2026 LAND!A1:N5`.
  - Note for Phase 3: Edge Functions run on Deno, so the production ingest code will use a Deno-compatible auth approach (lightweight JWT signing or `npm:googleapis` import), not the Node `googleapis` package this smoke test uses.

- **2026-05-11** — Supervisor decisions captured (recorded in PROJECT_BRIEF.md + DESIGN_DECISIONS.md):
  - Bank Deposit `2026 LAND`: column A is the real transaction date; columns L (second `DATE`) and M (status field) are out of scope; `CLIENT  NAME` double-space header is intentional.
  - Customer Support panel default brand filter: PPL (confirmed, mirrors H1 PDF).
  - Marketing Expense `Category` dropdown: supervisor agreed to add it.
  - 2026 tab in Marketing Team Reporting Template: supervisor agreed to create it.
  - Test user deleted from `auth.users` — only the real admin remains.

- **2026-05-11** — Canonical mapping drafted, sent to supervisor, returned approved:
  - `scripts/dump-bank-deposit-canonicals.mjs` + `pnpm dump:canonicals` extract unique PURPOSE + LOCATION values from `2026 LAND` (601 data rows scanned).
  - `data/canonical_mappings_bank_deposit_draft.md` is the supervisor-review doc; final approved canonical list locked at **20 PURPOSE** (27 raw variants in) + **24 LOCATION** (24 raw variants, no merges — Ire Mowe vs Ire Mowe Extension and Lavida Hills vs Lavida Prime confirmed distinct).
  - Security/Clearing sub-question resolved same day: Security Fee and Clearing Fee are conceptually distinct canonicals, but both 2026 source rows (`SECURITY FEE` and `SECURITY FEE / CLEARING FEE`) alias to "Security Fee" — treated as the same entity for this instance. Clearing Fee canonical created with no source-data alias yet; `CLEARANCE FEE` pre-seeded per supervisor spelling preference.

- **2026-05-11** — Migration 009 written and applied (`20260511000009_seed_canonicals.sql`):
  - Seeds `locations`, `location_aliases`, `purposes`, `purpose_aliases` from the approved list. Idempotent: `on conflict (name) do nothing` on canonicals, `on conflict (lower(alias)) do nothing` on aliases, alias FKs resolved via join on `name`.
  - Applied via Supabase SQL editor on the live project. Verification query returned `locations=24, location_aliases=24, purposes=20, purpose_aliases=28` — counts match exactly. Phase 2 fully closed.

- **2026-05-12** — Phase 3 deploy + first live ingest, plus two real-data fixes:
  - Migration 010 first invocation tripped Supabase's `safeupdate` extension on the unqualified `DELETE` inside `refresh_sales_by_location_monthly()` — even though the statement was wrapped in a `SECURITY DEFINER plpgsql` body. Migration 011 (`20260512000011_fix_aggregate_refresh_truncate.sql`) swaps `DELETE` for `TRUNCATE` which is the correct primitive anyway (faster, no MVCC overhead) and sidesteps the safety net by design.
  - First successful invocation revealed `unparseable_date: 397` (89% of rows). Root cause: (a) supervisor's ledger convention enters the date once per day and leaves column A blank for subsequent deposits on the same day; (b) ~60 dates are typed as text (`"13/01/2026"`, D/M/YYYY Nigerian convention) instead of as real date cells. Fixes shipped in the Edge Function:
    - `sheetsAuth.ts` now exports `parseDmyTextDate()` and a unified `parseSheetDate()` that tries serial-number first, then D/M/YYYY text. Calendar validation (`new Date(...)` round-trip) rejects Feb-30-style garbage.
    - `ingest-bank-deposit/index.ts` carries a `lastValidDate` across rows. Empty column A → forward-fill from the most recent parsed date. Non-empty but unparseable → flagged with `unparseable_date` and NOT forward-filled (so typos can't silently inherit a neighbouring date). Response payload now includes `forwardFilledDateCount` for ops triage.
  - Second invocation after the date fix: 448 rows, 126 aggregate buckets, `flagCounts.unparseable_date=1` (down from 397), `flagCounts.null_sales_person=253` (matches the brief's "~56% null SALES PERSON" prediction exactly: 253/448 = 56.5%), `forwardFilledDateCount=497` (counts forward-fills on data + blank rows; slightly noisy but a useful signal that the supervisor's date convention is heavy).
  - 6 duplicate TRANS CODES detected and disambiguated with `-row{N}` suffix (`SR-00168`, `PPL-00994`, `J-1151`, `PPL-01062`, `J-1156`, `PPL-01086`) — neither row in any pair lost.
  - 24 rows had blank TRANS CODE and got the `row-{N}` fallback id. Grep-able for ops follow-up.
  - Function deployed with `--no-verify-jwt`. Architectural rationale: the function is invoked by cron and an eventual admin-only "Refresh now" button; both contexts have no per-request user identity that matters. Internal auth still hard: service-role secret for DB writes, service-account JWT for Google Sheets. Captured in DESIGN_DECISIONS.md.
  - RLS verified working as intended: bank_deposits / sales_by_location_monthly are unreadable via the publishable (anon) key. Eyeball-level verification requires SQL editor or a signed-in admin JWT — not a regression, it's the correct posture for fact tables.

- **2026-05-11** — Phase 3 Bank Deposit ingest written (not yet deployed):
  - Decisions captured in DESIGN_DECISIONS.md → "Ingest Edge Function rules": Deno `crypto.subtle` for JWT sign, `UNFORMATTED_VALUE` Sheets render, TRANS CODE → `row-{N}` fallback for `source_row_id`, aggregate refresh via Postgres function called by RPC.
  - Header row of `2026 LAND` re-verified via the existing smoke script — confirmed columns A=`DATE`, B=`BANK STATEMENT DETAILS`, C=`AMOUNT`, D=`BANK ACCOUNT`, E=`PURPOSE`, F=`LOCATION`, G=`ACCOUNT PAYMENT NAME`, H=`TRANS CODE`, I=`CLIENT  NAME` (double space), J=`SALES PERSON`, K blank, L second `DATE`, M status (`ALERT SENT`).
  - `UNFORMATTED_VALUE` rendering confirmed via a throwaway one-shot: dates return as serial numbers (e.g. row showing `1/2/2026` D/M/YYYY came back as `46054` = 2026-02-01), amounts as numbers. Sheet locale is D/M/YYYY (Nigerian convention) but the Edge Function math is locale-free.
  - Migration 010 (`20260511000010_refresh_sales_aggregates.sql`) — `refresh_sales_by_location_monthly()` Postgres function: DELETE + INSERT GROUP BY inside a single plpgsql body, SECURITY DEFINER, search_path pinned. Returns row count. Granted to `service_role`. **Not yet applied** — pending SQL-editor run.
  - `supabase/functions/_shared/sheetsAuth.ts` — RS256 JWT sign via WebCrypto (`crypto.subtle.importKey` on PKCS8, `crypto.subtle.sign` with `RSASSA-PKCS1-v1_5`), token exchange against `oauth2.googleapis.com/token`, `readSheetValues()` helper (always uses `UNFORMATTED_VALUE` + `SERIAL_NUMBER`), `sheetsSerialToIsoDate()` helper (Lotus 1-2-3 epoch math).
  - `supabase/functions/_shared/canonicalLookup.ts` — loads `location_aliases` + `purpose_aliases` once per invocation into `Map<lower(alias), id>`; `lookupCanonical()` returns null for misses so the caller can emit `unknown_*` flags.
  - `supabase/functions/ingest-bank-deposit/index.ts` — main function. Named-column constants (`COL.DATE=0` etc.), `raw_row` preserves all 10 in-scope columns, dedup pass appends `-row{N}` to disambiguate duplicate TRANS CODES so neither row is silently lost, chunked upsert (500/chunk; ~426 YTD rows fit in one), `rpc('refresh_sales_by_location_monthly')` at the end. Response JSON tallies `rowsRead`, `rowsUpserted`, `blankSkipped`, `fallbackRowIdCount`, `duplicateTransCodes`, `flagCounts`, `aggregateRowsInserted`.

- **2026-05-14** — Marketing Expense ingest scaffolded (not yet deployed):
  - Both supervisor unblockers verified delivered via `scripts/inspect-sheet-structure.mjs` (new in this session — a generic tab-lister + range-dumper used to ground all subsequent code in real sheet shape). CATEGORY column at H on Jan–May 2026 tabs; `_Categories` tab holds the 11 H1 canonicals exactly. New `2026 Realtors Managers Weekly Report` tab exists with 3 month blocks (wide pivot: 8 cols/month = label + 5 weeks + total + gap).
  - Migration 012 (`20260514000012_seed_expense_categories.sql`) seeds `expense_categories` with the 11 H1 canonicals + `display_order`. Idempotent on `name` conflict. No alias table: typo / pre-dropdown values route through the TypeScript keyword fallback instead, keeping all category-mapping logic in one place.
  - Migration 013 (`20260514000013_refresh_marketing_monthly.sql`) ships `refresh_marketing_monthly()` — TRUNCATE-then-INSERT pattern locked in by migration 011, with two CTEs (`totals` for period-level income/expenditure, `by_cat` for the per-category breakdown) and a correlated scalar subquery into `jsonb_object_agg(expense_category_id::text, cat_total)` for the `by_category` jsonb map. v1 ingest only writes `entry_type='expenditure'` rows, but the function computes `total_income` via FILTER so it's forward-compatible if income ingest ever lands.
  - `supabase/functions/_shared/parseMarketingTab.ts` — `parseMarketingTabName()` (year/month from tab title, only `INGEST_YEARS` opt-in — currently `{2026}`), `findExpenditureHeader()` (scans first 10 rows for the `Date|Description|Total|Category` quad; verified consistent at row 4 across Jan/Feb/May 2026). Necessary because rows 1–3 carry stale "Petty Cash Book August" title text on every duplicated tab.
  - `supabase/functions/_shared/categoryFallback.ts` — 10 conservative keyword rules (most-specific first: SettleQuick → Genius → Realtor Manager → SMS → MSME → Stakeholders → digital-ad platforms → Social Media → Realtor → Market Storm). Anything unmatched falls to Miscellaneous. Every fallback-categorized row carries `fallback_category` in `quality_flags` with the rule that fired, so the data-quality view can grep them.
  - `supabase/functions/ingest-marketing-expense/index.ts` — discovers tabs dynamically via the new `getSheetTabs()` helper (added to `_shared/sheetsAuth.ts`), filters via `parseMarketingTabName()`, reads `A1:Q200` per tab, finds the header row, then parses each data row with forward-fill on `in_cell_date` (same convention as Bank Deposit since April 2026 mixes serial-number and D/M/YYYY-text dates). Summary rows (`Total`, `Balance c/f`, `Balance b/f`, `Balance b/d`) filtered out by description match. `source_row_id` is `exp-row-{N}` so future Income ingest can use `inc-row-{N}` without colliding on the unique constraint. Upsert + `rpc('refresh_marketing_monthly')`. Response payload tallies `tabsDiscovered`, `tabsIngested`, per-tab stats, `flagCounts`, `aggregateRowsInserted`.
  - Pending: apply migrations 012 + 013 via the Supabase SQL editor; deploy the function (`supabase functions deploy ingest-marketing-expense --no-verify-jwt` — same rationale as bank-deposit: cron + admin button, no per-request user identity).

- **2026-05-14** — Marketing Expense ingest deployed + first live run successful:
  - Migrations 012 + 013 applied via SQL editor.
  - First invocation needed `supabase secrets set SHEET_ID_MARKETING_EXPENSE=...` because only `SHEET_ID_BANK_DEPOSIT` was pushed in the previous session — a follow-up for every future ingest is to push its sheet-id secret as part of the deploy ritual.
  - Run stats: 22 tabs discovered, 5 ingested (Jan–May 2026), 94 rows upserted, 5 aggregate buckets, 0 unparseable dates, **94/94 rows flagged `fallback_category`** (every 2026 row has the dropdown blank, as expected from the inspection). Per-tab: Jan 13, Feb 29, Mar 20, Apr 26, May 6 rows. summarySkipped=2 on every tab (Total + Balance c/f filtered correctly).
  - Verification script `scripts/verify-marketing-ingest.mjs` added + wired to `pnpm verify:marketing`. Reads `marketing_monthly` + `marketing_expenses` and prints (a) per-month buckets, (b) category × month matrix with row counts + totals, (c) up to 5 sample descriptions per category so the keyword-rule choices can be spot-checked. Requires `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`, which is NOT currently set there (RLS blocks anon reads on fact tables) — supervisor must paste it locally before running, or run the equivalent SQL in the dashboard editor.

- **2026-05-14** — Customer Support canonical dump generated for supervisor review (`data/customer_support_canonical_inputs.md`):
  - Three decisions locked before the dump: (a) ingest **all dates** (no year filter — dashboard date range handles display, mirrors the H1 PDF's 2025 baseline), (b) **dump-then-approve workflow** identical to Bank Deposit canonicals, (c) only the **5 active reps** per `Rep_ID` (CATHERINE / MARIAM / MARY / YETUNDE / LOVINAL) — ABIDEMI and VICTORIA tabs ignored.
  - Dump stats: **10,214 populated rows scanned** across the 5 reps (CATHERINE 1809, MARIAM 1993, MARY 2005, YETUNDE 1855, LOVINAL 2552 — Lovinal is by far the largest log). **329 unique "Nature of Complaint" values, 39 unique channels, 12 unique statuses.** Lovinal has 137 unique complaint values (vs Mary's 41) — large free-text variation that the supervisor probably wants to collapse aggressively. Channels and Status are NOT planned for canonical mapping (stored as text on `customer_support_logs`); they're in the dump for the supervisor to eyeball for typo cleanup.
  - **Major design question for the supervisor**, surfaced in the dump but not pre-decided here: **composite multi-category cells**. Many rows hold values like `"Documentaion, Site Allocation"` or `"Semi-finished Delivery, Conversion to Land, Refund"`. The `customer_support_logs` schema today has a single `complaint_category_id` FK — composites don't fit. Three options for the supervisor to pick: (i) split into multiple `customer_support_logs` rows (one per category — increases row count), (ii) take the first category as primary + flag the row, (iii) treat composites as their own canonicals (rejected here — combinatorial explosion).
  - **Obvious typo clusters already visible** (won't need supervisor judgment): "Documentation" / "Documentaion" (one canonical, two aliases); "General Enquiry" / "General enquiries" / "Enquirires" / "Enquiry about plot status"; "Special Task" / "Special task"; "Adit" / "Audit"; "Futher payment on proprerty" / "Further payment"; "Authorisation" / "Authorization".
  - Script wired to `pnpm dump:cs-canonicals` for re-runs as the supervisor edits the source sheet.

- **2026-05-14** — Bank Deposit unparseable_date row investigated + closed without code change:
  - Row `PPL-00979` carried `DATE = "31/03/3036"` — supervisor typed year 3036 instead of 2026. `parseDmyTextDate` rejects years > 2100 (the existing guard), so the row got `unparseable_date` correctly. Action sits with the supervisor: fix the cell on the source sheet to `31/03/2026`. Next scheduled (or manual) ingest run will pick it up cleanly. No code change.

- **2026-05-14** — Customer Support ingest scaffolded (awaiting supervisor canonical seed before deploy):
  - **Composite strategy locked**: split into multiple `customer_support_logs` rows. Supervisor's reasoning — when a customer lodges multiple complaints in one log entry, each deserves its own row for accurate panel counts.
  - **Quality-flag vocabulary extended**: `UNKNOWN_COMPLAINT_CATEGORY` added to `_shared/quality_flags.ts`. Fires when "Nature of Complaint" had a non-empty value that didn't match any seeded alias. Empty Nature cells are NOT flagged — absence is its own state.
  - `_shared/canonicalLookup.ts` gained `loadComplaintAliases()` and `loadActiveCustomerServiceReps()` (returns lower-case-name → { id, brand_id } map for case-insensitive tab-name → rep_id resolution).
  - `_shared/parseCustomerSupport.ts` ships column constants for the A–N CS region (cols O–Q "resolution time" + R–AC "special tasks" deferred), the 5-rep tab list, and a quote-aware `splitComposite()` that respects `"…, …"` quoted single-category values.
  - `supabase/functions/ingest-customer-support/index.ts` — iterates the 5 active rep tabs, parses each row, splits composite Nature cells into N log rows with `source_row_id = row-{N}-{i}` (1-indexed split position), reuses the Bank Deposit date primitives, upserts in 500-row chunks, RPC-calls `refresh_customer_support_monthly`. Bails up-front with a clear error if any tab name fails to resolve to an active rep.
  - Migration 014 (`20260514000014_refresh_customer_support_monthly.sql`) — `refresh_customer_support_monthly()` RPC, TRUNCATE pattern, aggregate grain `(year, month, brand_id)`. Joins `customer_support_logs` → `customer_service_reps` for brand. **`resolved_count` defaults to strict `lower(trim()) = 'resolved'`** — RESPONDED is treated as "answered but not closed" and excluded. Supervisor can override in a follow-up migration if they want RESPONDED counted. `avg_resolution_minutes` is null in v1 (cols O–Q not yet read; supervisor input needed on time-zone / business-hours math).
  - **Migration 015 reserved** for the supervisor-approved `complaint_categories` + `complaint_aliases` seed. Until 015 lands, every ingested row will carry `unknown_complaint_category` — expected and correct first-deploy behaviour.
  - **Known v1 limitation**: no stale-row sweep. If the supervisor edits a composite cell from `A, B` to just `A`, the old `row-N-2` (B) record stays in the DB and would be counted by the panel. Documented in the function header; add a per-tab `delete where source_row_id not in (...)` sweep if it causes real issues.

- **2026-05-14** — CS canonical mapping draft sent for supervisor review:
  - Migration 014 applied to live project. `refresh_customer_support_monthly()` function exists; will return 0 inserted rows on first call (logs table is empty until ingest runs after 015).
  - `data/canonical_mappings_customer_support_draft.md` proposes the boiled-down canonicals from the 329 raw values, with 7 explicit decision points for the supervisor.

- **2026-05-14** — CS canonical mapping approved + migration 015 written:
  - Supervisor decisions: UK spelling on `Authorisation`; OneApp vs Realvest App kept separate; "Special Request — Documents" name accepted; Birthday Messages kept (part of CS comms); long-tail singletons (Default / Default Waiver / Downtime / ETRAC / Farmwey / Edificio) each promoted to own canonical; nothing missing. Change of Plot vs Change of Plot Size returned blank — defaulted to "kept separate" per the proposal (easy to merge later if needed).
  - **Final canonical count: 60** — 11 typo-cluster merges (Documentation, General Enquiry, Audit, Special Task, Refund, Commission Payout, Follow-Up, Authorisation, Further Payment on Property, Product Pricing Complaint, Collection of Document/Receipt) + 40 distinct complaint categories + 3 "Special Request" sub-types + 6 promoted singletons. Aliases: ~80 entries, each `(canonical_name, raw_value)` pair joined to the canonical via `name` in the migration.
  - Aliases preserve case-sensitive source spellings; trim is handled by the ingest's lookup, so trailing/leading whitespace variants do not need their own rows. Double-space and intentional-typo variants (`New  Contract`, `Pick Up Of  Doucments/Recipts`, `Futher payment on proprerty`) DO have dedicated alias rows.
  - Migration 015 (`20260514000015_seed_complaint_categories.sql`) written and ready to apply. Same idempotency pattern as migration 009 (canonicals `on conflict (name)`, aliases `on conflict (lower(alias))`).

- **2026-05-14** — Customer Support ingest deployed + first run **completely clean**:
  - Migration 015 applied. Secret `SHEET_ID_CUSTOMER_SUPPORT` pushed. Function deployed `--no-verify-jwt`.
  - First-run response: **10,763 log rows upserted across 5 rep tabs, 26 aggregate buckets** (≈13 months × 2 brands). **Zero unparseable dates, zero unknown_complaint_category, zero quality flags fired.** Every complaint value — across 329 raw uniques + countless composite-split atoms — matched a seeded alias. Validates both the 60-canonical / ~80-alias mapping AND the quote-aware composite splitter.
  - Per-tab logicals → upserted (composite-split delta): CATHERINE 1,809 → 1,911 (+102, 98 composite rows); MARIAM 1,994 → 2,083 (+89, 83 composite rows); MARY 2,008 → 2,057 (+49, 49); YETUNDE 1,855 → 1,903 (+48, 46); LOVINAL 2,552 → 2,809 (+257, 224 — Lovinal's tendency to log multiple complaints per entry confirmed).
  - Total run wall-time: ~12 seconds for the full sheet read + 22 chunked upserts + aggregate refresh.

- **2026-05-18** — Weekly Sales + Customer File ingests deployed in one session (closing step 3 except for Realtor Managers Weekly):
  - **Tab discovery via `inspect-sheet-structure.mjs`**: confirmed `2026 Weekly Sales Report` and `2026 Customer File` both live inside `SHEET_ID_BANK_DEPOSIT` (no new sheet share needed — same Bank Deposit Mirror sheet that already feeds `2026 LAND`). Shape grounded in real data before any code was written.
  - **`supabase/functions/_shared/parsePlotType.ts`** ships two parsers (per DESIGN_DECISIONS: plot-type matching lives in TypeScript, not SQL). `parseWeeklySalesPlotType(raw)` handles the `{count} {WORD}[ [{N}SQM]]` convention (`1 EXECUTIVE`, `2 CLASSIC`, `1 QUARTER`, `1 SPECIAL [220SQM]`); `parseCustomerFilePlotSize(raw)` handles `{N}SQM` and `{count} {UNIT}` (`600SQM`, `1 ACRE`, `1 HECTAR`). QUARTER/ACRE/HECTARE all bucket as Special per the brief. `loadPlotTypeLookup()` returns a `Map<PlotTypeName, uuid>` keyed on the four canonical names — same load-once pattern as the existing alias loaders. The Customer-File parser deliberately returns no `count` (plot count lives in a separate column H on that tab — the caller reads it from the column, not the cell text).
  - **Migration 016** (`20260518000016_plot_sales_aggregate.sql`) — `plot_sales_monthly` aggregate with grain `(period_year, period_month, location_id, plot_type_id)`; both FK columns nullable so `unknown_location` and `unparseable_plot_type` rows aggregate into "unknown" buckets rather than silently dropping. No unique constraint — the RPC TRUNCATEs and rebuilds (pattern locked in migration 011), and with two nullable FK columns a unique constraint would need three partial indexes. Columns: `plot_count` (sum), `txn_count` (count), `total_amount` (sum of `weekly_sales.amount` — the "payable" side per supervisor #1; `bank_deposits.amount_received` remains the "received" side, surfaced together never reconciled). RLS: authenticated-read, service-role-write, matches the migration 006 pattern. Applied via SQL editor on the live project.
  - **`supabase/functions/ingest-weekly-sales/index.ts`** — discovers date-marker rows (those `['', 46042]` rows with only a serial date in col B and everything else blank) via `isDateMarkerRow()` and skips them as visual-anchor noise rather than data. No forward-fill on this tab — every data row carries its own DATE in column G. RPC-calls `refresh_plot_sales_monthly` at the end. Response payload tallies `rowsRead`, `rowsUpserted`, `blankSkipped`, `dateMarkerSkipped`, `flagCounts`, `aggregateRowsInserted`.
  - **`supabase/functions/ingest-customer-file/index.ts`** — forward-fills col A on the same convention as Bank Deposit (date entered once per day, blanks below inherit). Two amounts mapped: `amount` ← INITIAL PAYMENT (col O), `amount_payable` ← TOTAL AMOUNT PAYABLE (col N). `APPROVED BY` (col L) holds CS-rep names (MARY/CATHERINE/MARIAM), NOT realtor-manager names — preserved in `raw_row` but `realtor_manager_id` stays null until a real source emerges. No aggregate refresh: Customer File is the customer-ledger / demographics source, not a headline-panel feed; aggregates can land in a later migration when a panel needs them.
  - **Deploys**: both functions deployed via `pnpm exec supabase functions deploy --no-verify-jwt --project-ref hrmrqpkcvyjwxrehrgvq`. Same `SHEET_ID_BANK_DEPOSIT` secret already in place from the 2026-05-12 session — no `secrets set` round-trip needed this time.
  - **First-run results, Weekly Sales**: 103 rows read, 46 upserted, 28 blank, 29 date-marker rows correctly skipped (math: 46+28+29=103). 27 aggregate buckets in `plot_sales_monthly`. Flags — `unknown_location=8` (8/46 = 17%), `unparseable_plot_type=2`, `null_sales_person=3`. Wall-time ~1.7s.
  - **First-run results, Customer File**: 122 rows read, 82 upserted, 40 blank. Forward-fill triggered exactly once (most rows have explicit dates in col A — the convention is looser on this tab than on `2026 LAND`). Flags — `unknown_location=73/82 = 89%`, `unparseable_plot_type=1`, `null_sales_person=1`. Wall-time ~1.2s.
  - **Big finding worth flagging to the supervisor**: 89% of Customer File rows hit `unknown_location`, dominated by `PALM ORIGIN` (a project name seen 8× in the first 20 rows during sheet inspection, not in the migration 009 canonical list). Plus `Mbirichi` (case-different spelling variant of seeded `MGBIRICHI`), `"IRE, Mowe"` (quoted-comma variant that exact-match alias lookup misses), `EWEKORO 2`, `IRE 2`. None block ingest — rows still upserted, location_id is null, raw value preserved in `quality_flags` AND `raw_row`. Cleanest fix: a Bank-Deposit-style canonical mapping draft sent to supervisor → migration 017 to seed. Tracking under "Open items".
  - **Cross-source sanity check (eyeball)**: same customer + date appears in all three sources with consistent figures — Nwaeze Amobi Oba on 2026-02-01: Bank Deposit ₦2.9M, Weekly Sales `1 EXECUTIVE @ MGBIRICHI ₦2.9M`, Customer File `600SQM @ Mbirichi ₦2.9M`. Numbers reconcile (so supervisor #3 surfaces zero discrepancies for this row); spellings diverge (Mbirichi vs MGBIRICHI) — an alias case, not a discrepancy.

- **2026-05-15** — Phase 4 React scaffold landed (step 4 of 9 in the roadmap):
  - **Design lock** via ui-ux-pro-max: Executive Dashboard style; B2B Service palette (slate-900 primary, slate-700 secondary, sky-700 `#0369A1` accent, slate-50 bg); Corporate Trust typography (Lexend headings, Source Sans 3 body — Lexend chosen for readability on the supervisor's phone). Encoded in `tailwind.config.js` as the `brand` 50–900 ramp + `accent` token; fonts preconnected and loaded in `index.html`.
  - **Auth flow**: `AuthProvider` in `src/hooks/useAuth.tsx` holds session + profile + status (`loading|signed-out|signed-in`), subscribes to `supabase.auth.onAuthStateChange`, loads the `profiles` row after sign-in. `ProtectedRoute` redirects unauthenticated users to `/sign-in` and preserves the requested path in router state for post-login redirect. `SignInPage` is the only public route; sign-up has no UI (admin-provisioned accounts only).
  - **Routes** (`src/App.tsx`, react-router v7): `/sign-in` public; `/sales`, `/marketing`, `/customer-support`, `/realtor-management`, `/media-content` protected behind `AppShell`; index redirects to `/sales`; `*` → 404 page. Anon key reads are zero on fact tables per RLS so the gate is real.
  - **Global date range** (`src/hooks/useDateRange.tsx` + `src/lib/dateRange.ts`): six presets (H1 2026 default, YTD, This Quarter, This Month, Last 30 days, H2 2025) plus custom. State persisted to URL search params (`?from=YYYY-MM-DD&to=YYYY-MM-DD`) — refresh-safe and shareable. `matchPreset()` reverse-derives the preset id from the current `from/to` so the chip label stays honest after a Custom edit.
  - **AppShell** (`src/components/AppShell.tsx`): sticky header with brand, `DateRangePicker` chip, and sign-out button (icon-only on mobile). Desktop ≥ md: collapsible 56-wide sidebar with full labels. Mobile < md: fixed bottom tab bar with 5 destinations (`Sales / Marketing / Support / Realtors / Media`) — sits at the iOS 5-destination guideline limit. Bottom-nav padding uses `env(safe-area-inset-bottom)` for iPhone home-indicator clearance. Header sticks at `top-0`, content wrapper has `pb-24 md:pb-10` so the bottom-nav doesn't cover the last card.
  - **DateRangePicker** (`src/components/DateRangePicker.tsx`): chip trigger shows preset name when one matches, formatted range otherwise. On mobile the popover renders as a bottom sheet (`fixed inset-x-3 bottom-3`) with a backdrop; on desktop as a 320px-wide popover anchored to the chip. Custom range uses native `type="date"` inputs (iOS shows the wheel picker, desktop a calendar). Closes on outside click and Escape; trigger refocuses on Escape.
  - **Section placeholders**: every page uses `SectionHeading` (title + subtitle + current range) and a `StatusBanner` describing the actual ingest state today (Sales/Marketing/Support → "ingest live" with row counts; Realtor Management / Media & Content → "pending"). `PlaceholderCard`s show grey rectangles where real charts will go. No fake numbers shown anywhere — the supervisor sees an honest state.
  - **Icons** hand-rolled inline SVG in Lucide style (`src/components/icons.tsx`) — zero extra deps. Per ui-ux-pro-max checklist: no emoji icons, 24×24 viewbox, consistent stroke.
  - **Type check**: `tsc -b` exits 0 under `strict + noUnusedLocals + noUnusedParameters`. Production build: 425.80 kB JS / 123.18 kB gzipped (Recharts dominates the bundle — revisit if it hurts the supervisor's phone load time once a real chart is wired).

- **2026-05-18 (design-lock session)** — Sales (Land) panel layout signed off; no code shipped this session, but the layout-decisions buffer was emptied so next session can be straight implementation:
  - **H1 2025 PDF read end-to-end** via `pdftotext -layout`. The Sales section runs 11 distinct views: H1 plots × size grid; per-plot-sold chart; by-location chart; month-on-month table (Total Payable / Initial Received / Further Payment); by-quarter (Q1 vs Q2 per location); 2024 vs 2025 YoY; Q1 vs Q2 chart; H1 realtor-sale-tier wide table (sub-1M / 1–5M / 5–10M for both initial-deposit and total-payable bases); top-selling-location-by-realtor leaderboard; OneApp Customer Interaction Tracker; observations narrative. Of those, **6 are backable by 2026 data we have today**; YoY needs 2025 ingest, realtor tiers + leaderboard need per-customer realtor attribution, OneApp is Phase 2.
  - **Purpose canonicals confirmed splittable.** Migration 009's 20 canonical purposes carry consistent stage-prefixes: `Initial Land / Initial D&D / Initial Doc Levy` + `Outright Land / Outright D&D` group as "Initial Received"; `Further Land / Further D&D / Further Doc Levy` + `Balance Land / Balance D&D / Balance Doc Levy` group as "Further Received". Maps 1:1 onto the supervisor's H1 PDF narrative ("further payment gave us ₦125.8M more than initial deposit"). The remaining 7 fee canonicals (Allocation Fee, Change of Ownership, Business Rep Reg, Security, Clearing, Client Deposit, Property Flex, Default Charge, Book Purchase) stay out of headline KPIs in v1 — they're surfaced as a footnote line "Fees & Charges: ₦x" rather than folded into Received.
  - **Layout locked: 6 cards, phone-first vertical stack.** (A) KPI strip — Plots Sold / Total Payable / Initial Received / Further Received, 2×2 mobile / 1×4 desktop. (B) Month-on-Month stacked-bar chart — Initial + Further as stacked fill, Payable as a thin tick line behind each stack so the visible gap = unpaid balance. **No threshold callouts**; supervisor #3 is honored by visual encoding alone (a threshold-triggered callout is itself a soft reconciliation — system editorialising). Permanent muted "Owed ₦x" label under each month so every gap is shown, not just the >20% ones. (C) Plots × Size pivot — locations rows desc by plot_count, 4 size cols + total, sticky first column + `overflow-x-auto` on phone. (D) Revenue-by-location — top 8, two horizontal bars per row (payable + received) + delta chip, "+ 16 more with ₦x combined" above the "Show all 24" expander so hidden locations aren't invisible. (E) Q1 vs Q2 — paired horizontal bars per location (NOT a table — DB-recommended for phone scan), Q2 bar gets `StatusChip amber "in progress"` until 2026-06-30. (F) Three greyed cards each naming the specific blocker (OneApp / YoY / realtor tiers) rather than a generic "coming soon".
  - **Style direction (within existing brand+accent lock, no restyle).** Card frame: `rounded-2xl border border-slate-200 bg-white p-4 md:p-6`. No shadows (B2B sales-intelligence is flatter; shadows fight density). KPI value: `text-2xl md:text-3xl font-bold text-slate-900 tabular-nums`. Label: `text-xs uppercase tracking-wide text-slate-500`. Source footnote: `text-[10px] text-slate-400`. Accent: sky-700 primary series, slate-700 secondary, slate-400 tertiary/received. **Red banned from the panel** — discrepancies are information, not errors. Amber-600 reserved for `in-progress` / `pending source` chips only. New `StatusChip` mini-component with slate/sky/amber variants — JSX pattern captured in the Commit 1 next-session entry below.
  - **DB recommendations applied** (from `ui-ux-pro-max` `search.py` runs):
    - B2B Service → Sales Intelligence Dashboard style (matches existing palette — no restyle needed).
    - Compare Categories → bar chart primary; descending sort; value labels on bars.
    - Trend over time → line chart with 20% fill opacity for multiple series (drove the choice to use stacked-bar + tick-line for MoM rather than 3 separate lines, since at 360px width 3 overlapping lines lose precise tick targets).
    - Mobile responsive table → `overflow-x-auto` wrapper (Card C confirmed).
    - Focus states `focus:ring-2`; disabled state `opacity-50 cursor-not-allowed` (greyed cards in Card F).
  - **Three-commit implementation order locked.** Commit 1: KPI strip + MoM card + StatusChip + `src/lib/queries/sales.ts`. Commit 2: plots pivot + revenue-by-location bars. Commit 3: quarter pair + greyed cards. Reviewable progress at each step; supervisor can eyeball numbers against the source sheet before next commit lands.
  - **One open data question deferred to Commit 1.** Today's Bank Deposit ingest doesn't yet tag a row by its "stage prefix" — that classification happens at query time by joining `bank_deposits.purpose_id` → `purposes.name` and prefix-matching the canonical name. Two design choices for Commit 1: (a) prefix-match in TypeScript after fetching purpose IDs once at mount, or (b) add a generated `stage` column or view in a new migration 017. Lean toward (a) for v1 since the canonical list is 20 rows and the mapping rule is simple — adding a column ties schema changes to a UI decision that should remain UI-side per the "plot type matching lives in TypeScript" precedent from DESIGN_DECISIONS.

- **2026-05-20** — Sales panel Commit 1 shipped + auth hardening:
  - **KPI strip + MoM card live on `/sales`.** Four KPIs (Plots Sold, Total Payable, Initial Received, Further Received) in a 2×2 mobile / 1×4 desktop strip. Fees & charges (the 9 non-stage canonicals) surfaced as a separate footnote line — not folded into Received (honors supervisor #3). MoM card uses Recharts `ComposedChart` with stacked Bars (Initial sky-700 + Further slate-700) and a dashed step-after Line for Payable; visual gap = unpaid balance, per the design lock's "no threshold callouts" rule. Custom XAxis tick renders "Owed ₦x" directly under each month so labels stay aligned at any month count (custom date ranges work, not just H1's six bars).
  - **Purpose-stage classification implemented in TypeScript, not SQL.** `classifyPurposeName()` in [src/lib/queries/sales.ts](src/lib/queries/sales.ts) prefix-matches `Initial|Outright` → initial, `Further|Balance` → further, else fee. Picked option (a) from the design-lock open question (TS-side over migration 017 — keeps schema decisions out of UI-driven groupings, per the `parsePlotType` precedent). Backed by a single one-shot fetch of `purposes` at mount + a `Map<purpose_id, stage>` lookup.
  - **`PanelCard`** ([src/components/PanelCard.tsx](src/components/PanelCard.tsx)) ships the locked frame `rounded-2xl border border-slate-200 bg-white p-4 md:p-6`, no shadow. **`StatusChip`** ([src/components/StatusChip.tsx](src/components/StatusChip.tsx)) ships slate/sky/amber variants. **`src/lib/format.ts`** holds `formatNaira` / `formatNairaCompact` / `formatMonthShort` / `formatAsOf` (used across the panel). Sales-page placeholders updated to describe Commits 2/3 + the three greyed-out blockers (OneApp / YoY / realtor tiers).
  - **Pagination + freshness on the read path.** `loadSalesPanelData` paginates `bank_deposits` 1000 rows at a time (future-proof if H1 grows), filters `plot_sales_monthly` year-bounded then refines month-bounds in JS (it's tiny — 27 rows today). Tracks the latest `updated_at` on bank_deposits + `refreshed_at` on plot_sales_monthly; KPI card surfaces "As of …" via a slate StatusChip.
  - **Build**: 798 kB JS / 232 kB gzipped (up ~80 kB from the previous 425 kB — Recharts `ComposedChart` pulled additional code). Worth a code-split pass if the supervisor's phone load time suffers; not blocking now.
  - **Auth hardening (independent, observed during user smoke-test on a work Chrome profile).** Two issues fixed:
    - [src/hooks/useAuth.tsx](src/hooks/useAuth.tsx) had no error/timeout path around `supabase.auth.getSession()`. If the call hung, `status` stayed `'loading'` and the app showed the Loading screen forever. Added an 8s `withTimeout` race that falls through to `signed-out` on reject — guarantees the user reaches the sign-in screen.
    - [src/lib/supabase.ts](src/lib/supabase.ts) now passes a custom in-memory lock (`inMemoryLock`) to the auth client instead of the default `navigator.locks`-based one. On one user Chrome profile (work-email profile, single window, no other localhost tabs) the navigator.locks call hung indefinitely — likely a corrupted Lock Manager state or extension interference. processLock wasn't a clean import (it's in the transitive `@supabase/auth-js` package, not re-exported by `@supabase/supabase-js`), so wrote a 15-line in-memory serializer instead. Single-tab dashboard, so the trade-off (two tabs could race during token refresh) is acceptable and self-recovers via SDK retry.
    - User-side validation: dashboard loaded cleanly, KPI strip + MoM card rendered real numbers, date-range chip ("This month") correctly re-queried.

- **2026-05-20 (cont.)** — Weekly Sales location aliases extended (migration 017 applied + ingest re-run):
  - Pivot's "Unknown / unmapped" row in the Sales panel investigated. Five raw values across 8 Weekly Sales rows (17%) accounted for everything: `IRE MOWE` (4 rows, ₦223M), `IRE, MOWE 1` (1, ₦50M), `IRE 2` (1, ₦8M), `EWEKORO 1` (1, ₦6M), `EWEKORO 2` (1, ₦1.3M).
  - Supervisor decisions: IRE MOWE / IRE, MOWE 1 → existing canonical `Ire Mowe`; IRE 2 → `Ire Mowe Extension`; EWEKORO 1 / 2 → `Ewekoro`. No new canonicals — all five fold into existing seeds.
  - Plus: add `IRE MOWE [EXT]` as an alias alongside the existing `IRE MOVE [EXT]` (kept the typo alias for already-ingested 2026 LAND rows; the new alias matches the corrected spelling for future entries).
  - Migration 017 (`20260520000017_extend_location_aliases.sql`) applied via SQL editor; `ingest-weekly-sales` invoked via curl (anon-key Authorization, function deployed `--no-verify-jwt`). Post-run: `unknown_location` flag count dropped from 8 → 0; aggregate row count 27 → 26 (the Unknown bucket disappeared).

- **2026-05-20 (cont.)** — Sales panel Commit 2 shipped + small sign-in tweak:
  - **PlotSizePivot card.** Rows = locations sorted by total plot_count desc, cols = Starter / Classic / Executive / Special / Total. Mobile uses `-mx-4 overflow-x-auto` wrapper + `position: sticky; left: 0` on the location column with a 1px shadow as the visual separator so the column reads as pinned when other cols scroll. Zero-count cells show an em-dash rather than `0` to keep the table scannable. Unknown-location rows aggregate into one "Unknown / unmapped" row at the appropriate sort position.
  - **RevenueByLocation card.** Side-by-side horizontal bars per row: Payable (slate-400, from `plot_sales_monthly.total_amount`) and Received (sky-700, from `sales_by_location_monthly.amount_received`). Two sources surfaced together, never reconciled (supervisor #3). Right-hand chip shows "Owed ₦x" or "Paid in full" — keyword `Owed`, not "deficit"/"shortfall", to match the supervisor's H1 PDF language. Top-8 by default, expander reveals the rest with a "+N more — ₦x payable / ₦y received combined" line so hidden tail is at least visible in aggregate.
  - **Query layer**: `loadSalesPanelData` now also returns `pivot` + `byLocation`. Added one fetch (`fetchSalesByLocationMonthly`) and extended `fetchPlotSalesMonthly` to embed `location:locations(name)` + `plot_type:plot_types(name)` via PostgREST FK embeds. Single mount-time fetch path stays consolidated (parallel `Promise.all`); page-level loading state covers all four cards uniformly. Bundle: 806 kB JS / 234 kB gzipped (+2 kB over Commit 1 — no new dependencies).
  - **SignInPage** ([src/pages/SignInPage.tsx](src/pages/SignInPage.tsx)) gained a show/hide password eye button — `IconEye` / `IconEyeOff` added to [src/components/icons.tsx](src/components/icons.tsx), button uses `type=button` so it doesn't submit, `aria-pressed` reflects state, `pr-11` on the input reserves room.

- **2026-05-20 (cont.)** — Revenue-by-location null-bucket fix (commit `15f1dc2`):
  - The card was showing an "Unknown / unmapped" row with ~₦138M received and ₦0 payable, rendering as "PAID IN FULL" — misleading. Those rows are Bank Deposit entries with no LOCATION cell on the source sheet — by design, fees and allocation/security/change-of-ownership payments don't belong to a specific land project.
  - `loadSalesPanelData` now partitions the null-location bucket out of `byLocation` and returns its total separately as `byLocationOtherReceived`. RevenueByLocation surfaces that total as a footnote ("Plus ₦X received without a location tag — typically fees & general deposits …") — value stays visible (supervisor #3), just removed from a list whose axis it doesn't belong on.

- **2026-05-20 (cont.)** — Inline drill-down across all four Sales cards (commit `d84a0c4`):
  - Supervisor request: tap any KPI / chart bar / row → see exactly where the number came from. Interaction locked as inline accordion (vs modal / sub-route) for mobile one-thumb usability; one drill open per card at a time.
  - **`loadSalesPanelData` extended** to precompute drill structures in the same pass (no extra queries). New types: `PurposeBreakdownEntry`, `PlotTypeBreakdownEntry`, `KpiBreakdowns`. `SalesMonthBucket` got per-month `initialBreakdown` / `furtherBreakdown` / `payableBreakdown`. `PlotPivotRow` got `monthly: PivotMonthlyEntry[]`. `RevenueByLocationRow` got `monthly: LocationMonthlyEntry[]`. `loadPurposeStages` now also returns `nameById` so per-purpose totals get human labels.
  - **Shared helpers**: [src/components/sales/BreakdownList.tsx](src/components/sales/BreakdownList.tsx) (label + display + mini-bar list, reused by KPI strip + MoM card); [src/components/sales/DrillPanel.tsx](src/components/sales/DrillPanel.tsx) (slate-50 accordion container with title slot); `IconChevronRight` added.
  - **KPI strip**: each of the 4 tiles is now a button. Click → accordion below the strip shows: Plots → by plot type; Total Payable → by plot type (count + amount); Initial → 5 canonicals (Initial Land/D&D/Doc Levy + Outright Land/D&D); Further → 6 canonicals (Further L/D&D/DL + Balance L/D&D/DL). The Fees & charges footnote is also now disclosable (shows the 9 fee canonicals).
  - **MoM chart**: bars are clickable; below the chart a row of month chips (sky-700 when selected). Selected month opens a 3-column drill: Initial breakdown / Further breakdown / Payable-by-plot-type. The XAxis label for the selected month bolds + turns sky-blue.
  - **Pivot rows**: clicking a row reveals an inline sub-row (colspan-full) with the month-by-month plot-type grid for that location. Chevron rotates 0° → 90°; the open row + sticky cell gets an `accent/5` background tint.
  - **Revenue-by-location rows**: clicking opens a DrillPanel with month-by-month payable/received bars + per-month "Owed ₦x" or "Paid in full" status. Same bar component as the top-level rows, scaled smaller via a `compact` prop.
  - **Build**: 819 kB JS / 237 kB gzipped (+3 kB gzip over Commit 2 — small price for 4 cards' worth of interaction).

- **2026-05-20 (cont.)** — Sales panel Commit 3 shipped — Sales panel now feature-complete:
  - **Q1 vs Q2 paired bars per location.** New [src/components/sales/QuarterPair.tsx](src/components/sales/QuarterPair.tsx) — per-location pair of horizontal bars (Q1 slate-400, Q2 sky-700), sorted by max(Q1, Q2) desc, top-8 with "Show all" expander and hidden-tail aggregate footnote ("+N more — ₦x Q1 / ₦y Q2 combined"). Per-row delta chip `+/-N% vs Q1` (emerald when up, slate when flat/down; suppressed when Q1=0 to avoid divide-by-zero noise). Card-header `StatusChip` flips between amber `"Q2 in progress"` and slate `"Q1 & Q2 final"` based on `today vs year-06-30`. Source line appends `(partial)` to Q2 total when in-progress. Empty state for ranges with no Q1/Q2 months. **Year drives off `range.to.slice(0,4)`** so the card auto-tracks the date picker (e.g. picking H1 2025 once that's ingested would re-key the comparison). **Source-of-truth posture:** uses `received` from Bank Deposit only (supervisor #1 — financial source of truth); same-metric across two periods is a comparison, not a reconciliation, so supervisor #3 stays honored. Zero new queries — derived in-component from existing `byLocation[].monthly[]`.
  - **Greyed blocker cards.** New [src/components/sales/GreyedCard.tsx](src/components/sales/GreyedCard.tsx) — slate-200 frame matching PanelCard with `opacity-50 cursor-not-allowed` + `aria-disabled="true"`, internal "Data source pending" tile. Each card names its *specific* blocker (per design-lock + ui-ux-pro-max DB rule): OneApp → "Awaiting AWS API access (Phase 2)"; YoY → "2025 source data not yet ingested"; Realtor sale tiers → "Per-customer realtor attribution pending — sub-1M / 1–5M / 5–10M tiers from H1 PDF". Lined up in a 3-col grid on desktop, single column on mobile.
  - [src/pages/SalesPage.tsx](src/pages/SalesPage.tsx) — last four `PlaceholderCard`s replaced with `QuarterPair` + three `GreyedCard`s; `PlaceholderCard` import dropped.
  - **Build**: 823 kB JS / 238 kB gzipped (+4 kB JS / +1 kB gzip over the Commit 2 + drill-down state — small price for one new card and the three blockers).

- **2026-05-21** — Supervisor review round 1: 4 dashboard asks + plot-count fix + revenue reconciliation:
  - **Hero "Total Revenue Inflow" KPI** added above the 4-tile KPI strip. Value = Initial + Further + Fees (Bank Deposit 2026 LAND). Clickable → 3-stage drill. Layout decision: hero card above 2×2 grid, not 5th tile (keeps the grid tidy and makes the headline number unmissable). Wired through `SalesKpis.totalRevenueInflow` + `KpiBreakdowns.totalRevenueInflow` in [src/lib/queries/sales.ts](src/lib/queries/sales.ts) — single new derived value, no new query.
  - **MoM chart segmented toggle** (Received vs Payable / Total Revenue) on the right of the card header in [src/components/sales/MoMChart.tsx](src/components/sales/MoMChart.tsx). Default view unchanged (Initial+Further stacked + Payable line). Total Revenue view stacks Initial + Further + Fees as one bar per month; under each month label it shows the month's total instead of "Owed". Drill panel adapts per view. Toggle resets the selected-month drill when switched. Required adding `fees` + `feesBreakdown` + `totalRevenue` to `SalesMonthBucket`.
  - **Plot pivot column totals row** — `<tfoot>` with column-wise sums for Starter / Classic / Executive / Special / Grand Total in [src/components/sales/PlotSizePivot.tsx](src/components/sales/PlotSizePivot.tsx). Sticky-left mirrors the location column behavior on mobile scroll.
  - **73 vs 71 plot reconciliation** — supervisor's standalone sales dashboard reported 73 plots; ours reported 71. Root cause: the regex in [supabase/functions/_shared/parsePlotType.ts](supabase/functions/_shared/parsePlotType.ts) `parseWeeklySalesPlotType` was rejecting `'1 ACRE'` (ACRE not in WORD alternation, row went `unparseable_plot_type=null`) and `'1 SPECIAL[cornerpiece]'` (bracket required `{N}SQM` content). Fix: WORD alternation extended to `STARTER|CLASSIC|EXECUTIVE|SPECIAL|QUARTER|ACRE|ACRES|HECTAR|HECTARE|HECTARES` (all non-canonical units bucket as Special per the brief); bracket content is now permissive `[^\]]*` with size extraction only when the content is `{N}SQM`. After re-deploy: `unparseable_plot_type=0`, plots sold = 73 ✓.
  - **Per-location deal-count badge** on RevenueByLocation rows — supervisor's dashboard format `IRE MOWE · 80` (revenue · deals). Sourced from `bank_deposits.location_id` counted at query time (no new aggregate needed). Null-location footnote also surfaces the untagged transaction count.
  - **New Top Realtors card** ([src/components/sales/TopRealtors.tsx](src/components/sales/TopRealtors.tsx)) — bar leaderboard sorted by revenue (top 5 default, expand to top 10). Sourced from `bank_deposits.sales_person` aggregation. Multi-token names auto title-cased; single-token sentinels (`STAFF`, `SMR`, `ASSETPLUS`) left as-typed. Null SALES PERSON rows bucket into an "Unattributed" entry per the brief's "~56% null" prediction.
  - **New Top 5 Deals card** ([src/components/sales/TopDeals.tsx](src/components/sales/TopDeals.tsx)) — single-largest bank-deposit transactions with client / date / location / purpose / salesperson on each row. Multi-instalment deals appear once per receipt (supervisor #3 — no silent reconciliation).
  - **New Weekly Detail card** ([src/components/sales/WeeklyDetail.tsx](src/components/sales/WeeklyDetail.tsx)) — Mon–Sun week selector (defaults to most recent), expand to reveal 6-column transaction table (Date / Client / Location / Purpose / Salesperson / Amount). Week list resets correctly when the global date range changes. All rows derived from the same paginated `bank_deposits` fetch the rest of the panel uses — no extra queries.
  - **Query layer extensions** in [src/lib/queries/sales.ts](src/lib/queries/sales.ts): `fetchBankDeposits` now selects `location_id, customer_name, sales_person` (in addition to the existing `txn_date, purpose_id, amount_received, updated_at`); added `fetchLocationNames()` for the `locationNameById` lookup; new derived structures `TopRealtorEntry`, `TopDealEntry`, `WeekBucket`, `dealCount` on `RevenueByLocationRow`, `byLocationOtherDealCount` on `SalesPanelData`. Week bucketing uses Mon-Sun UTC math (no Date timezone gotchas).
  - **Layout** on `/sales` ([src/pages/SalesPage.tsx](src/pages/SalesPage.tsx)) now: Hero KPI → 4-tile KPI strip → MoM (toggle) → Plot Pivot → RevenueByLocation → QuarterPair → **Top Realtors + Top 5 Deals (2-col) → Weekly Detail** → 3 greyed blockers. Build: 840 kB JS / 241 kB gzipped (+17 kB JS / +3 kB gzip from the 823 / 238 pre-session baseline — three new cards + the new derived structures).

- **2026-05-21 (cont.)** — Bank Deposit revenue reconciliation: ₦465.64M → ₦673M (matches supervisor's reference dashboard ±row-count differences):
  - **Discovery**: Hero showed ₦465.64M (H1 2026); supervisor's reference Apps Script dashboard showed ₦670.2M unfiltered. Gap of ₦200M+. Initial hypothesis was 114 future-dated rows (dates Jun-Dec 2026) being filtered out by H1 range — but those weren't typos in the surface sense; the cells held actual `46174` (June 1) serial-number dates the BANK MIRROR had written. Bank writes M/D/YYYY into a D/M/Y-locale sheet, so e.g. "01/06/2026" intended as Jan 6 gets stored as serial June 1. Plus year-3036 typos (`PPL-00979` text "31/03/3036", and sheet row 316 serial 415071 decoding to year 3036).
  - **Approach 1 (abandoned)**: Two-pass section-anchor algorithm — collect text-format dates with D>12 as immovable monthly anchors, then for each serial-number date, if its month doesn't match the nearest anchor but day-month swap does, swap. Plus year-force when parsed year > 2100. Got the dashboard from ₦465M → ₦666M, but introduced complexity (~140 lines) and one stubborn row (sheet 316) couldn't be auto-corrected because neither the forced nor the swap-forced date matched its section month.
  - **Approach 2 (shipped)**: Switch date source from column A (bank's auto-paste, dirty) to **column L** (supervisor's clean working ledger). Column L has the correct dates throughout — sequential, no swap issues, no year-3036 typos. The brief's 2026-05-11 "column A is the real txn date" decision is superseded by this session; CLAUDE.md and DESIGN_DECISIONS.md updated to reflect. Section-anchor + M/D/Y + year-correction code deleted (~140 lines removed). `MDY_CORRECTED` and `YEAR_CORRECTED` quality flags dropped from the vocabulary.
  - **Column L lag**: supervisor's L column lags the bank's auto-paste by a few days (last L value = May 16; bank's column A has entries through May 20). **Tail fallback**: a first pass finds `lastLNonBlankRow`; only rows AFTER that index fall back to column A when L is blank. Within L's coverage, L-blank rows forward-fill from L as before (this avoids M/D/Y-poisoned column-A serials like sheet rows 483 / 606 / 610 which decode to August / October / November 2026 from the bank's M/D/Y typos — using their A values would corrupt forward-fill chains). Fallback rows get a new `date_fallback_to_a` quality flag carrying the original column-A raw value for audit.
  - **`COL` constants** in [supabase/functions/ingest-bank-deposit/index.ts](supabase/functions/ingest-bank-deposit/index.ts): `DATE_A: 0` (preserved for traceback, written into `raw_row` as `DATE A`); `DATE: 11` (the new primary). `RAW_ROW_KEYS` extended to length 12 with a `null` slot at index 10 (the blank header column K).
  - **Final ingest run**: 472 rows upserted, 3 `date_fallback_to_a` (May 18/19/20 tail), 0 `unparseable_date`, 1 `unknown_location`, 86 aggregate buckets. Dashboard `H1 2026` hero = ~₦673M against supervisor's ₦671.2M. The ~₦2M overage is a row-count difference (we have 472 rows; supervisor's Apps Script dashboard shows 459 records — likely 6 duplicate TRANS CODES the supervisor's script dedupes that we preserve via `-row{N}` disambiguation, plus ~7 blank-TRANS-CODE rows the supervisor's script skips that we ingest via `row-{N}` fallback id). Decision: leave our number more inclusive (every real money movement is counted; supervisor #3 favors visibility over silent filtering).
  - **One residual**: sheet row 316 (DAVID OLANIYI OJO, INITIAL D&D, ₦835K) has bank-mirror column A serial 415071 decoding to year 3036 — supervisor's intent is March 6, 2026, recoverable from column L (which has `46087` = 2026-03-06). Now correctly placed via column L.
  - **Scripts added**: [scripts/sum-bank-deposit-amount.mjs](scripts/sum-bank-deposit-amount.mjs) sums column C against the source sheet with the same parsing logic as the ingest, breaks down by month/purpose, dumps remaining out-of-range rows. [scripts/diag-l-blank-rows.mjs](scripts/diag-l-blank-rows.mjs) lists every row where L is blank but A has a value — used to validate the tail-fallback watermark. Both are ops tools, not wired into `package.json` scripts.

- **2026-05-25** — Marketing + Customer Support + Realtor Management panels shipped in one session (closes step 6 of 9):
  - **Marketing panel — 2 commits, feature-complete.** Design lock came out of ui-ux-pro-max: B2B Service + Trust & Authority (matches existing palette), part-to-whole → horizontal bars (DB rule rejects pie for >5 items), trend over time → bars (over line for 5 data points + larger tap targets). Two commits: (1) [src/lib/queries/marketing.ts](src/lib/queries/marketing.ts) (`loadMarketingPanelData` — single pass over `marketing_monthly` + `marketing_expenses` deriving KPIs + per-category aggregates with drill data + per-month buckets) + [MarketingKpiStrip](src/components/marketing/MarketingKpiStrip.tsx) (hero Total Spend + 4 tiles with DrillPanel below each) + [SpendByCategory](src/components/marketing/SpendByCategory.tsx) (top-8 + expander, per-row drill into monthly trend + top-3 sample descriptions, amber `Keyword-fallback {n}/{total}` chip surfacing the CATEGORY-dropdown gap supervisor #2/#3); (2) [MonthlySpendChart](src/components/marketing/MonthlySpendChart.tsx) (single sky-700 series, custom `SelectableBar` shape that switches fill on selection while keeping rounded-top corners, `LabelList` value labels above each bar) + 3 `GreyedCard`s (Billboard cost ₦19.95M H1 2025 ref, Activities-with-metrics table, Income side).
  - **Customer Support panel — 2 commits, feature-complete.** Hybrid data source: skipped the `customer_support_monthly` aggregate because its `resolved_count` is over ALL logs (enquiries + complaints) whereas the H1 PDF frames resolution rate as resolved-complaints / total-complaints. `loadCsPanelData` queries `customer_support_logs` directly with date+brand filter (paginated 1000-at-a-time), classifies enquiry-vs-complaint client-side, derives every metric in one pass. Brand toggle ([BrandToggle](src/components/customer-support/BrandToggle.tsx)) is panel-level state (segmented control PPL / RealVest / All — PPL default per H1 PDF), single source of truth, no per-card duplication. Commit 1: query layer + brand toggle + hero+4-tile [KPI strip](src/components/customer-support/CsKpiStrip.tsx) (Enquiries / Complaints / Resolved / Resolution Rate, each clickable into DrillPanel) + [EnquiriesByChannel](src/components/customer-support/EnquiriesByChannel.tsx) bars + [ComplaintsByCategory](src/components/customer-support/ComplaintsByCategory.tsx) bars with **emerald-resolved overlay on slate-total** stacked bar + per-row drill (monthly trend + sample raw text strings). Commit 2: [CsMonthlyTrend](src/components/customer-support/CsMonthlyTrend.tsx) (Recharts BarChart — enquiries standalone bar in sky, complaints stacked with slate-700 unresolved + emerald-600 resolved overlay) + 3 `GreyedCard`s (Avg resolution time — cols O–Q need time-zone math, migration 014 sets `avg_resolution_minutes = NULL`; Per-rep breakdown — Phase 2; CSAT — no source today).
  - **Realtor Management v1 — 1 commit, honest-placeholder layout.** Locked path after supervisor offered Q (built ingest first / honest placeholders / skip to Media / pause for review) — chose placeholders. New [PendingPanelCard](src/components/realtor-management/PendingPanelCard.tsx) component (amber-border frame with "Ingest pending" chip + bullet list of metrics each card will render) for the three cards that ship when `realtor_manager_weekly` ingest deploys: Recruitment Metrics (6 metrics from the wide pivot), Activity Measurement (Realtor Corner + Stakeholders Meeting), Monthly Trend chart. Existing `GreyedCard` (slate, permanently out-of-scope) for the three deferred items: Per-manager performance (Mrs Kemi / Richard Makava / Debbie only existed MAY/JUNE 2025 in H1 PDF), Newly Onboarded Realtors Digital Ad (same), OneApp prospect interactions (Phase 2 with AWS access). Two-section layout with `<h2>Ships when ingest deploys</h2>` + `<h2>Out of v1 scope</h2>` headers makes the distinction unmissable.
  - **Supervisor review of CS Monthly Trend** flagged chart colors too similar (sky-700 enquiries + sky-500 resolved overlay both blue). Palette tightened in [src/components/customer-support/CsMonthlyTrend.tsx](src/components/customer-support/CsMonthlyTrend.tsx), [src/components/customer-support/ComplaintsByCategory.tsx](src/components/customer-support/ComplaintsByCategory.tsx), [src/components/sales/MoMChart.tsx](src/components/sales/MoMChart.tsx): **Resolved → emerald-600** everywhere (distinct hue, reads as positive outcome — replaces sky-500 in CS and sky-700 in Complaints overlay), **Sales MoM Fees → violet-500** (third tone distinct from sky Initial + slate Further, replaces sky-500 that was too close to Initial), **CS unresolved → slate-700** (darker for contrast against new emerald overlay). Amber-600 stays reserved for pending chips only.
  - **Global refresh button + sign-out relocation** in same commit. New [src/hooks/useRefresh.tsx](src/hooks/useRefresh.tsx) — counter-bump context with `refresh()` action + `refreshing` flag held for 600ms floor so spinner stays visible on fast loads. `<RefreshProvider>` wraps protected routes in `App.tsx`. Every data-loading page (Sales / Marketing / CS) added `refreshCounter` to its `useEffect` deps. Header refresh button (top-right, "Refresh" / "Refreshing…" with `animate-spin`). Desktop sign-out moved out of header → sidebar-bottom card (avatar + display name + "Sign out" hint + log-out icon, `mt-auto` pushes it to viewport bottom). Mobile keeps compact icon-only sign-out in header (no sidebar to host it). New `IconRefresh` in [src/components/icons.tsx](src/components/icons.tsx).
  - **Sidebar pinning fix** in follow-up commit. Supervisor flagged sidebar "moving up and down" during scroll. Root cause: `sticky top-[73px]` was below the sidebar's natural y position (61px header + 32px pt-8 = 93px), so during the first 20px of scroll the sidebar drifted up with the content before pinning. Set `top-[93px]` to match natural position → sticky activates from scroll=0 with zero motion. Height `calc(100vh - 117px)` keeps a 24px bottom inset for breathing room around the sign-out card.
  - **Build trajectory:** 840 kB / 241 kB (Sales baseline) → 855 kB / 244 kB (Marketing C1) → 860 kB / 246 kB (Marketing C2) → 881 kB / 249 kB (CS C1) → 887 kB / 250 kB (CS C2) → 893 kB / 251 kB (palette + refresh + sign-out). +53 kB JS / +10 kB gzip across the whole step 6 — Recharts already loaded for Sales, so the marginal cost of each new chart is low.
  - **One GitHub-side hiccup** during commit-push sequence: pushes returned 500 Internal Server Error for ~10 minutes mid-session (`Request ID 9546:3218FD:...`). All commits were locally clean; pushes succeeded on retry once GitHub recovered. No code changes needed.

- **2026-05-20 (cont.)** — Auth scope tightened to tab-only:
  - **Problem reported by user**: closing the tab and re-opening localhost loaded straight into the dashboard with no sign-in prompt. Desired semantics: closing the tab requires a fresh sign-in; refreshing the same tab stays signed-in.
  - **Fix**: [src/lib/supabase.ts](src/lib/supabase.ts) now passes `storage: window.sessionStorage` to `createClient`, alongside the existing in-memory lock. Supabase auth defaults to `localStorage` which persists across tab closes; `sessionStorage` is scoped per tab — wiped on close, preserved through reload — exactly the semantic the user wanted. `persistSession: true` and `autoRefreshToken: true` explicitly set (both already SDK defaults; pinning so the intent is loud).
  - **Trade-off documented in code**: a second simultaneously-open tab will require its own sign-in (sessionStorage is per-tab). Acceptable for a single-tab supervisor dashboard.
  - **One-time effect on next load**: any user who had a session in `localStorage` from before this change will be signed out on next visit (SDK now looks in `sessionStorage` and finds nothing). The orphaned `localStorage` token entry is harmless dead data; user can clear it via DevTools if they want a clean slate, but it never gets read again.

- **2026-05-25 (cont.)** — Realtor Managers Weekly ingest landed end-to-end, closing step 3 of the roadmap. Realtor Management panel now reads live data:
  - **Schema redesign (migration 018).** Dropped the wrong-shaped `realtor_manager_weekly` table from migration 004 (it was scaffolded as one-row-per-(manager, week) with typed cols `recruitments` / `attendance_count` / `sales_count` / `sales_amount` + NOT NULL `realtor_manager_id`). The actual 2026 source tab is a wide weekly pivot whose ROWS are metric labels and COLS are weeks within month blocks — no per-manager dimension at all (locked v1 scope per 2026-05-14). New tables: `realtor_metrics_monthly` at grain `(period_year, period_month, metric_key)` + `realtor_metric_canonicals` (19 metrics across 3 categories — 6 recruitment / 3 activity / 10 sales perf) + `realtor_metric_aliases` (literal source spellings; the "Payabale" typo on the 6M+ tier preserved verbatim until the supervisor fixes it on the sheet). Weekly granularity preserved in `week_values` jsonb so a future weekly-trend card doesn't need a schema change. Reference table `realtor_managers` (Mrs Kemi / Richard Makava / Debbie) untouched — Phase 2 per-manager data lands in a separate table whenever a recurring source appears.
  - **Wide-pivot parser** ([supabase/functions/_shared/parseRealtorMetricsTab.ts](supabase/functions/_shared/parseRealtorMetricsTab.ts)). Month blocks are 8 cols wide (label + Week 1–5 + Total + gap) and the supervisor's row offsets between bands aren't consistent (e.g. Apr's labels start at col I row 42 while Mar's are at col A row 40), so the parser anchors on "Week 1" header cells — `labelCol = weekOneCol - 1`, month determined by scanning up to 5 rows upward for a recognizable month name in the block's col span. Each metric row → one fact row at month grain. NIL/Nil/NIl/Nll/Nl → 0 (event happened, count was zero; supervisor-locked 2026-05-25). Empty → null. Other strings → 0 + `non_numeric_value` flag with raw value preserved. Section headers + text-only rows (Site Inspection / Feedbacks / Bottlenecks / Special Events) silently skipped via SKIP_LABELS; unrecognized labels accumulated into `unknownLabels` response diagnostic without per-row flags. Computed week-sum wins for the fact-table `total`; source's manual Total preserved in `raw_row.Total`; disagreement emits `total_mismatch` quality flag with both numbers (supervisor #3).
  - **Quality flags vocabulary extended.** [supabase/functions/_shared/quality_flags.ts](supabase/functions/_shared/quality_flags.ts) gained `non_numeric_value` and `total_mismatch`. [supabase/functions/_shared/canonicalLookup.ts](supabase/functions/_shared/canonicalLookup.ts) gained `loadRealtorMetricAliases`.
  - **Edge Function** ([supabase/functions/ingest-realtor-managers-weekly/index.ts](supabase/functions/ingest-realtor-managers-weekly/index.ts)) discovers month blocks via the parser, stamps `source_row_id = y{YYYY}-m{MM}-{metric_key}` (stable across re-runs and supervisor row-reorders), chunked upserts at 500/chunk. Response payload tallies `blocksFound`, `rowsParsed`, `rowsSkipped`, `unknownLabels`, `flagCounts`, `nonNumericValues`, `totalMismatches`. Deployed `--no-verify-jwt` (cron + admin button, no per-request user identity — same architecture as every other ingest). Pushed all `.env.local` secrets to Supabase via `supabase secrets set --env-file` (one-shot fixes the per-ingest `SHEET_ID_*` push step that previously needed manual repetition).
  - **First ingest run**: 4 blocks (Jan–Apr 2026), 67 rows upserted, 32 section/text rows skipped, 6 unknown labels (free-text "Special Events" sub-rows the supervisor adds dynamically — `Site Inspection to Palm Orign Ewekoro`, `Special Events (Smart Money Reset - Realtor Special)`, `Farmwey Inspection`, `Product Briefing`, `Farmwey Storm`, `Special Events`), 1 `total_mismatch` on Apr "Number of New Business Reps" (week sum 6 vs source 7 — caught a supervisor math error, fixed in-sheet mid-session; re-run cleared the flag), 1 `non_numeric_value` on Mar Stakeholders Meeting Week 4 (`'SMR SPECIAL =85'` — supervisor double-typed the SMR Special event count there instead of leaving it for the Special Events row that already captures the 85; not data loss, just noise).
  - **Query layer + UI**. [src/lib/queries/realtor.ts](src/lib/queries/realtor.ts) — `loadRealtorPanelData` single-pass aggregation over `realtor_metrics_monthly` filtered by date range, hydrates `recruitment` / `activity` / `salesPerf` `MetricRow[]` ordered by `display_order`, plus `monthlyTrend` buckets with per-bar drill payload. `mergeActivityForDisplay()` synthesizes a `weekly_realtor_meeting` row that sums `master_class_1` + `master_class_2` per month, with the originals attached as `subRows` (DB canonicals stay separate per Phase 2 hygiene; merge is UI-only per supervisor 2026-05-25). [MetricMonthlyTable](src/components/realtor-management/MetricMonthlyTable.tsx) — shared table with sticky-left metric col, chevron-toggled row-drill that renders sub-rows indented under the parent. [RealtorMonthlyTrend](src/components/realtor-management/RealtorMonthlyTrend.tsx) — stacked BarChart (New Referrals sky-700 + New Business Reps slate-700 = New Realtors), per-bar + month-chip click → DrillPanel with `BreakdownList` of all recruitment + activity metrics that month. Tooltip surfaces stack-total vs source-reported new-realtors total when they diverge (don't reconcile).
  - **Page swap.** [src/pages/RealtorManagementPage.tsx](src/pages/RealtorManagementPage.tsx) replaced its three amber `PendingPanelCard`s with live `MetricMonthlyTable` (Recruitment) + `MetricMonthlyTable` (Activity, with the merged Weekly Realtor Meeting) + `RealtorMonthlyTrend`. Three out-of-v1 `GreyedCard`s preserved. `PendingPanelCard.tsx` deleted — single-use component, no longer referenced. StatusBanner now flips between "no data in this range" guidance and the standard error toast.
  - **Supervisor decisions captured 2026-05-25** (sync mid-session):
    1. "Realtor Corner" is dead in the 2026 source; Real Estate Master Class 1 and 2 are separate programs.
    2. UI label = "Weekly Realtor Meeting" (one row, merged from Master Class 1 + 2) with row-drill exposing the 1 vs 2 split. Implemented; both canonicals stay in DB.
    3. Apr "New Business Reps" total mismatch was a math error; supervisor fixed the Total cell on the sheet mid-session; re-ingest cleared the flag.
  - **Build trajectory**: 893 / 251 (Realtor placeholder baseline) → 902 / 253 (cards + query) → 904 / 253.6 (merge + row-drill). +11 kB JS / +2.6 kB gzip for the whole milestone — Recharts already loaded for Sales, so the marginal cost stays small.

- **2026-05-27** — Docs-only session: surfaced a v1 launch gap and made it impossible to miss in future sessions.
  - **Trigger**: user asked whether ingested data is live/real-time. Audit showed the dashboard's existing header refresh button (`useRefresh.tsx`) only re-queries Supabase tables — it does **not** re-invoke the ingest Edge Functions. There's also no cron deployed (three of the ingest files carried stale `Scheduled (every 15 min) via Supabase cron — Phase 3 wiring TBD` comments from May; nothing was ever wired). Net: today, the dashboard never reflects a Google Sheet edit until someone manually invokes the ingest function. That's a HARD launch blocker we hadn't flagged on the roadmap.
  - **Roadmap fix**: inserted a new **step 9 — Automated ingest scheduling + admin "Re-pull from Sheets" button** into the 9-step build order, renumbering "Polish, Netlify deploy" to step 10. Step 9 covers (a) pg_cron or Supabase Scheduled Functions hitting each of the six ingest Edge Functions on a 15-min cadence, (b) an admin-role-gated "Re-pull from Sheets" button in the shell that invokes all six ingests then bumps `useRefresh()`. The existing header refresh button keeps its current role (re-query Supabase only — fast, doesn't burn the Sheets API quota on every supervisor tap); the new admin button is the explicit "pull fresh data" path.
  - **Doc updates**: [PROJECT_BRIEF.md](PROJECT_BRIEF.md) build order extended to 10 items with the new step 9 spelled out. [PROGRESS.md](PROGRESS.md) Status / Build order overview / Current focus / Next-session entry points all switched from `of 9` → `of 10`; new entry-point bullet #4 explicitly calls out step 9 as the launch blocker so it's first thing seen next session. [CLAUDE.md](CLAUDE.md) "Step 6 of 9 done" line updated to "Step 6 of 10 done" with the caveat about the refresh button. Three ingest function headers ([ingest-bank-deposit](supabase/functions/ingest-bank-deposit/index.ts), [ingest-customer-support](supabase/functions/ingest-customer-support/index.ts), [ingest-marketing-expense](supabase/functions/ingest-marketing-expense/index.ts)) had their stale `Phase 3 wiring TBD` comments replaced with `wired in step 9 of the roadmap (HARD launch requirement; not yet deployed)` so the file-level vocabulary matches the roadmap. Dated milestone entries that reference `step N of 9` deliberately left as-is — they record what was true at the time they were written.
  - **No code changes.** Step 9 itself remains to do; this session only made sure we won't forget.

- **2026-06-01** — Step 9 closed: on-demand Sync Sheets button + 15-min pg_cron schedule. Driven by a 3pm presentation deadline (boss demoing to his bosses) — the manual button is what fires in the room; the cron is what keeps data fresh between sessions.
  - **Why both pieces matter, separately.** Cron at 15-min cadence keeps Supabase silently up-to-date so the dashboard is never more than ~15 min stale. The on-demand button is the "I just edited the sheet, I want it NOW" override for live presentations. The existing lightweight `Refresh` button (re-query Supabase tables only — fast, free) stays in the header alongside the new `Sync Sheets` button; their titles spell out the trade-off so the user picks the right one ("Re-query Supabase tables (fast). Use Sync Sheets to pull from Google.").
  - **Access gate.** Originally scoped as admin-only per the roadmap; relaxed mid-session to sign-in-only after the user pointed out admin-gating risks locking the boss out (he may not be flagged admin). Sign-up is admin-provisioned, so every signed-in user is already trusted. Edge Functions are deployed `--no-verify-jwt` anyway, so the gate doesn't add security — only friction.
  - **`RepullButton` UX** ([src/components/RepullButton.tsx](src/components/RepullButton.tsx)). Single button in the header next to Refresh: cloud-download icon, label "Sync Sheets". Click → opens an inline popover and kicks off all 6 ingests in parallel via `supabase.functions.invoke()`. Popover shows a one-row-per-ingest list with: spinner while running, ✓ + `rowsUpserted` count when done, ⚠ + truncated error message on failure. Total wall-time stays ~12–15s (the longest ingest is Customer Support at ~12s; the rest finish faster but run concurrently). Button state colors reflect outcome: idle (white border), running (sky accent), all-ok (emerald), partial-fail (amber). After completion the button calls `useRefresh()` so every active page re-queries Supabase tables and renders the freshly-pulled data. Mobile is icon-only; popover anchors right-aligned to the trigger and stays inside the 300px column.
  - **`rowsUpserted` extraction**: each ingest returns its own payload shape — most expose a top-level `rowsUpserted` number, but `ingest-marketing-expense` aggregates per-tab (`tabs[].rowsUpserted`). [extractRowsUpserted()](src/components/RepullButton.tsx) sums the per-tab variant when the top-level field is missing. Failures show error message instead.
  - **`supabase.functions.invoke()` over raw fetch** keeps the auth header logic centralized (it picks the session JWT or anon key automatically) and gives consistent `{ data, error }` semantics. Both SDK errors and function-returned `data.error` paths are caught separately so a 500 with an error payload looks the same in the UI as a network-level failure.
  - **`AppShell` wiring** ([src/components/AppShell.tsx](src/components/AppShell.tsx)) — the new button slots between `DateRangePicker` and the existing `Refresh` button. Refresh's title now reads "Re-query Supabase tables (fast). Use Sync Sheets to pull from Google." to make the difference explicit.
  - **Migration 019** ([supabase/migrations/20260601000019_schedule_ingest_cron.sql](supabase/migrations/20260601000019_schedule_ingest_cron.sql)) ships the cron half. Stack: `pg_cron` (job scheduler) + `pg_net` (async HTTP) + `vault` (secret storage). The migration installs a `public.invoke_ingest_function(fn_name)` SECURITY DEFINER helper that resolves two Vault secrets (`supabase_functions_base_url`, `supabase_anon_key`) and POSTs to the named function via `net.http_post` with a 60s timeout, then schedules 6 cron jobs (one per ingest) staggered by 1 minute across the 15-min cycle (Bank Deposit at minutes 0/15/30/45; Marketing at 1/16/31/46; etc.). Staggering means each Sheets ingest gets its own minute, so Sheets API quotas and Supabase function CPU never spike on the same tick. Net: 24 invocations / hour.
  - **Prerequisite (one-time, ops-side)**: migration header documents two `vault.create_secret(...)` calls that have to be run in the SQL editor BEFORE applying 019 — these store the project's functions base URL and the anon key. Anon key is already public (it ships in the frontend bundle); Vault is used for cleanliness + rotatability, not cryptographic secrecy. The `--no-verify-jwt` deployment posture still means the gateway accepts any signed JWT, so the anon key suffices.
  - **Idempotency**: extension creates use `if not exists`; the helper function uses `create or replace`; jobs are unscheduled in a DO block before re-creation so re-applying the migration replaces cleanly instead of failing on the `cron.job` unique-name constraint.
  - **Verification queries** are inline in the migration footer: list active jobs from `cron.job`, recent runs from `cron.job_run_details`, response bodies from `net._http_response`.
  - **Build**: 911 kB JS / 255 kB gzipped (+18 kB JS / +4 kB gzip from the post-step-6 baseline of 893 / 251 — one new component + two icons).
  - **No code change to ingest functions.** The headers of `ingest-bank-deposit`, `ingest-customer-support`, `ingest-marketing-expense` still carry the comment "wired in step 9 of the roadmap (HARD launch requirement; not yet deployed)" — those are slightly stale after this session but accurate for the codebase audit (the cron lives in the migration, not in the function code). Worth a one-line cleanup pass next session.

- **2026-05-29** — Supervisor signed off on dashboard progress; handover documentation landed.
  - **Trigger**: supervisor reviewed current progress and approved direction, with one concern raised — after the SIWES intern leaves, the company needs a written standard for how data is entered into the source Google Sheets so the ingests don't break silently when staff change or sheets get reshuffled.
  - **Deliverable**: [docs/data-entry/](docs/data-entry/) — handover data-entry standards split per department. Six files: [README](docs/data-entry/README.md) (index), [00-common.md](docs/data-entry/00-common.md) (universal rules + canonical-additions process + troubleshooting + glossary; everyone reads first), and one per active department — [01-sales-and-finance.md](docs/data-entry/01-sales-and-finance.md) (Bank Deposit Mirror — 3 tabs), [02-marketing.md](docs/data-entry/02-marketing.md) (Marketing Fund Expense), [03-customer-support.md](docs/data-entry/03-customer-support.md) (MASTER SHEET - CUSTOMER SUPPORT), [04-realtor-management.md](docs/data-entry/04-realtor-management.md) (Realtors Managers Weekly tab).
  - **Authored against actual ingest behaviour, not aspiration.** Headers, tab names, COL positions, and quality-flag triggers pulled directly from each `ingest-*/index.ts` and `_shared/parse*.ts` so the doc reflects what the code actually reads. Covers: tab-name + header rules (do-not-rename / do-not-reorder), date-cell and money-format rules, the intentional `CLIENT  NAME` double-space header, why column L (not A) is the Bank Deposit date source of truth, the `Month YYYY` tab convention on Marketing, the all-caps per-rep tabs on Customer Support, the wide-pivot Total-vs-week-sum behaviour on Realtor Management, and the "type the new value, supervisor adds the alias" workflow for new LOCATION / PURPOSE / Complaint Category values.
  - **Structural choice**: drafted first as a single master document at the root, then split per department after supervisor preference for team-specific hand-out copies. Each per-team doc opens with a `read-00-common-first` callout so universal rules aren't duplicated. Old root-level `DATA_ENTRY_STANDARDS.md` deleted (uncommitted draft, superseded).
  - **Owner / contact line** in `00-common.md` §5 left as a fill-in placeholder for handover-time.
  - **No code changes.**

## Current focus

**Steps 6 + 9 of 10 done.** Step 9 (automated ingest scheduling + on-demand Sync Sheets button) shipped 2026-06-01 against a 3pm same-day presentation deadline. Header now carries Sync Sheets (re-pull from Google) + Refresh (re-query Supabase) side-by-side; pg_cron fires all 6 ingests every 15 min via migration 019 (vault-backed URL + anon key, staggered minute offsets). Sales remains the reference panel after supervisor review round 1 (₦673M H1 2026 reconciliation). Marketing + CS + Realtor all rendering with the same drill-down rhythm. Only step 10 (polish + Netlify deploy) remains before launch, with step 7 (Media & Content forms) optionally landing first.

## Next-session entry points

1. **Apply migration 019 to the live project + create the Vault secrets.** Migration is written but not yet applied; the live cron starts firing once these are done in the SQL editor. Order matters — run the `vault.create_secret(...)` calls first (header of [migration 019](supabase/migrations/20260601000019_schedule_ingest_cron.sql) has the exact statements), then apply the migration body. Verification queries are inline at the migration footer: confirm 6 jobs in `cron.job`, wait one tick (~15 min) and inspect `cron.job_run_details` + `net._http_response` for HTTP 200s. If pg_cron or pg_net aren't enabled at the project level, enable them under Database → Extensions in the Supabase dashboard before running the migration.
2. **Media & Content (step 7 of 10).** Manual-entry forms for PG (FB/IG/YT), PPL (FB/IG), RealVest (FB/IG), Genius (IG only). Schema already exists (`social_media_manual_entries` table from migration 007). Different work from the chart panels — CRUD forms with brand × platform × month grids, validation, optimistic writes, editor-role RLS. Design lock pending — run ui-ux-pro-max against the H1 Media & Content section of the PDF before coding.
3. **Eyeball verification on phone.** Marketing + CS + Realtor + the new Sync Sheets popover have not yet been viewed in a browser this session — build is green but the supervisor reviews on phone. Walk through `/marketing`, `/customer-support`, `/realtor-management` on a phone before next supervisor sync. Specifically check: SelectableBar custom shape rendering at narrow widths (Marketing); truncated "Largest category" tile on phone; emerald-resolved overlay readability in CS Monthly Trend; the Realtor `MetricMonthlyTable` sticky-left + chevron-row-drill on the Weekly Realtor Meeting row; sidebar-bottom sign-out card's tap target on touch laptops; **NEW**: Sync Sheets popover anchor positioning on a 360px viewport (300px popover + right-edge padding).
4. **Bundle-size pass (low priority).** 255 kB gzipped after step 9. Once Media & Content is wired, code-split Recharts into a lazy chunk and benchmark on the supervisor's phone load time.
5. **Tidy the stale `wired in step 9` comments** in `ingest-bank-deposit`, `ingest-customer-support`, `ingest-marketing-expense` headers. The cron now lives in migration 019, not in the function code — those one-line comments are accurate-but-misleading after this session.

**Standing items not on the critical path:**

- **PALM ORIGIN + Customer-File location canonicals** — 73/82 first-ingest rows fired `unknown_location`. Bank-Deposit-style dump + supervisor review + new migration. Not blocking any current panel (Sales v1 doesn't read Customer File).
- **Netlify deploy of the React shell** — env vars ready; defer until supervisor sign-off on the three new panels.
- **`PPL-00979` unparseable_date** in `bank_deposits` (year typed as `3036`). Action sits with the supervisor.
- **Per-manager realtor attribution** — wide pivot has no per-manager columns; v1 Realtor panel locked aggregate-only.
- **Realtor source-data noise (NEW, 2026-05-25)** — 6 free-text "Special Events" sub-rows the supervisor added inline below the standard metric rows (`Site Inspection to Palm Orign Ewekoro`, `Special Events (Smart Money Reset - Realtor Special)`, `Farmwey Inspection`, `Product Briefing`, `Farmwey Storm`, `Special Events`). Surfaced in the ingest response's `unknownLabels` but not blocking — no fact rows created. Worth offering the supervisor a structured "special events" sub-section if they want these tracked, or asking them to keep these out of the recurring weekly rows. Same for the `'SMR SPECIAL =85'` text in Mar Stakeholders Meeting Week 4 (double-entry; the SMR Special count is captured cleanly on the dedicated Special Events row).

## Open items waiting on supervisor

- [x] Add `Category` dropdown column to Marketing Fund Expense source sheet — **delivered 2026-05-14**. Landed at column H on Jan–May 2026 tabs (those tabs went 26 → 27 cols; older tabs unchanged). A new `_Categories` tab holds the canonical list with all 11 H1 values matching the brief. Existing 2026 rows have CATEGORY blank — supervisor added the column but did not backfill, so `fallback_category` keyword matching is required as planned.
- [x] Create 2026 tab in Marketing Team Reporting Template — Realtor Managers Weekly Report — **delivered 2026-05-14**. Tab is named `2026 Realtors Managers Weekly Report` (note "Realtors" plural — different from the legacy `Realtor Managers Weekly Report` tab). Confirmed wide-pivot layout: each month block = label col + Week 1–5 + Total + gap col (8 cols per month). Three month blocks populated so far (Jan/Feb/Mar 2026). No per-manager breakdown visible in the top rows — that's a follow-up question for the supervisor before realtor managers ingest lands, but does not block Marketing Expense ingest.
- [ ] Surprise gotcha to flag on next supervisor sync: rows 1–3 of `May 2026` carry stale title text (row 1 = `"Marc"`, row 2 = `"Petty Cash Book August"`) — the tab was duplicated for May without updating the in-cell title. Headers live at row 4. Ingest will anchor on the header row, not assume row 1, so this is cosmetic only — but worth pointing out so it doesn't propagate into future months.
- [ ] **Customer File location canonicals (NEW, 2026-05-18)** — 73/82 first-ingest rows fired `unknown_location`. Dominated by `PALM ORIGIN` (new project name not in migration 009's 24-location seed), plus spelling/punctuation variants of seeded canonicals (`Mbirichi` for MGBIRICHI, `"IRE, Mowe"` quoted, `EWEKORO 2`, `IRE 2`). Needs a dump-and-approve cycle: extract uniques → supervisor confirms canonical decisions (new entries vs aliases) → migration 017. No data loss — every flagged row is upserted with `location_id=null` and the raw text preserved in `raw_row` + `quality_flags.unknown_location`.
